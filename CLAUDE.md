# Developer Notes

## Getting Started
- Dev server: `npm run dev`
- Type check: `npm run typecheck`

## Conventions
- Strict TypeScript everywhere — no `any`
- ESM only — relative imports must include explicit `.ts` extensions
- No enums — use literal union types instead